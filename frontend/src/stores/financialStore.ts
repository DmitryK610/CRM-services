// Временно скрыто для избежания ошибок сборки
// Financial store будет реализован позже

import { defineStore } from 'pinia'

export const useFinancialStore = defineStore('financial', {
  state: () => ({
    // Пустое состояние
  }),
  actions: {
    // Пустые действия
  }
})

